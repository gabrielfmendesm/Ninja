{
  "name": "unas",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "react-scripts start",
    "electron": "electron .",
    "package": "electron-packager . --overwrite --platform=darwin --arch=arm64 --out=dist/ --icon=path/to/icon.icns",
    "package-win": "electron-packager . --overwrite --platform=win32 --arch=x64 --out=dist/ --icon=path/to/icon.ico"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^32.0.1",
    "electron-packager": "^17.1.2"
  }
}
